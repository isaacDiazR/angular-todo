services:
  cyberpunk-todo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cyberpunk-todo-app
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
